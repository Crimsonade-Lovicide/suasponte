Genesis: designed, built, and shipped the suasponte.dev public docket; live with health 200.

## What this cycle was

The genesis session (operator present, instructions in the session, not
a normal BRIEF cycle). Chose the site concept: a public docket ruled on
sua sponte by the memoryless hourly maintainer. Anyone files motions
anonymously (random key, hash stored); the court rules in public;
every governance act goes to an append-only log enforced by D1 triggers.

## What was read

OPERATOR.md (no instructions yet), STATE.md (fresh repo), NEEDS_HUMAN.md
(empty), BRIEF.md in full.

## What shipped

- Worker (src/index.js + src/lib.js): home/docket/motion/log/canon pages,
  /health, JSON API (motions list/file/get, log, canon), zero client JS,
  strict CSP, all user text HTML-escaped (tested).
- D1 schema (migrations/0001_init.sql): motions + log, log append-only
  by trigger — verified refusing UPDATE/DELETE both locally and remotely.
- Rate limits in code as public policy: 5/key/hour (verified live-local:
  6th filing -> 429), 12/site/10min, 500-pending cap. No IPs stored, ever.
- scripts/gavel.mjs (queue/rule/strike/log), pulse.sh, 10 unit tests.
- Deployed once; custom domain suasponte.dev attached; health 200.
- Motion #1 filed and granted end-to-end as the live smoke test; genesis,
  deploy, and the ruling are log entries 1-3.
- README.md, BRIEF.md site-specifics section, this log, STATE.md.
- Hourly routine scheduled: trig_01Uua6dSfehtRHhLLNairw5M, hourly at :27 UTC,
  fresh session per fire, each reads BRIEF.md and follows it on main.

## What was skipped and why

- No KV namespace: D1 covers rate limiting with fewer moving parts; the
  plan allows KV if a future granted motion needs it.
- No comments-on-motions in v1: keeps genesis surface small; a natural
  first motion for someone to file.
- NEEDS_HUMAN.md untouched: no choice arose that the genesis mandate did
  not already settle.
