Quiet cycle (seventeenth): 0 pending motions, same nine stale PRs, no ship.

What I read:
- OPERATOR.md: still just the 2026-08-22 "no instructions yet" placeholder.
  No new dated entries.
- STATE.md (sixteenth cycle's notes) and NEEDS_HUMAN.md: read in full for
  what the last cycle left behind and what's already parked for the
  operator. Nothing there is treated as verified fact this cycle (rule 7)
  without re-checking it below.
- `pulse.sh`: tripped ("pulse: open github issues"), same recurring
  condition every cycle has hit since the stale-PR pile started (the
  probe checks presence of any open GitHub issue-API item, which
  includes PRs, not novelty) — so the full cycle ran.
- `node scripts/gavel.mjs queue`: 0 pending motions.
- `GET https://suasponte.dev/api/motions?status=pending`: `{"motions":
  [],"next_after":null}` — agrees with gavel.
- `GET https://suasponte.dev/health`: 200,
  `{"ok":true,"service":"suasponte","version":"genesis-1","checks":
  {"d1":"ok"}}`.
- GitHub issues API paged to exhaustion (page 1 of 3, per_page=100):
  page 1 had all 9 open items, pages 2-3 empty. All 9 are pull requests,
  zero conventional issues. Numbers/titles unchanged from the sixteenth
  cycle's read: #2 (unsolicited `ecc-tools[bot]` bundle, STOP-listed,
  already documented in NEEDS_HUMAN.md) plus #3-#10 (prior maintainer
  cycles' own draft bookkeeping-only PRs, already superseded by `main`).
  No new PR, no new issue.
- Re-fetched PR #2's file list directly this cycle (rule 7, not trusted
  from prior notes): same ten files as previously documented
  (`.claude/ecc-tools.json`, `.claude/identity.json`,
  `.claude/homunculus/...`, `.codex/config.toml` wiring MCP servers
  including `mcp.exa.ai`, etc.) — unchanged, still squarely rule 8
  (edits `.claude/`, external service/credential/network access).

What I shipped: nothing. No motion, no issue narrative, nothing to
reproduce or fix. `npm test`: 14/14 green (baseline, re-run this cycle,
no code touched so no re-run needed after). No code changed, no
migration, no deploy (rule 10 — a quiet cycle is a good cycle).

What I skipped and why: closing or commenting on any of the 9 open PRs.
BRIEF reserves PR/issue closing to the operator (already the consistent
position across every prior cycle's notes back to the fifth cycle); I
have no explicit authorization to do otherwise, so I left all nine
untouched, same as every prior cycle since #2 first appeared. No new
NEEDS_HUMAN.md entry was needed — the existing entries covering #2 and
the stale-bookkeeping-PR pile (now #3-#10, one more than last recorded)
already state the recommendation and remain accurate; nothing changed
enough to warrant appending a new one this cycle.

Branch/PR note for this cycle's own bookkeeping: this session pushes its
cycle record directly to `main`, per BRIEF's own ending-a-cycle
instructions and matching how every prior cycle's commits actually
landed on `main` (verified via `git log` on `origin/main` this cycle,
not assumed). No new `claude/*` branch or PR was opened for this cycle's
own record, so the stale-PR count should not grow because of this run.
