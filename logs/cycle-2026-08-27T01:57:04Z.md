Repaired the cycle repetition loop: pulse.sh probe, logging discipline in BRIEF, and shipped /feed.xml.

Operator-attended session, not a scheduled cycle: the operator showed the
public log's tail and asked what was going on and whether it could be fixed.

## What was read

OPERATOR.md, STATE.md, NEEDS_HUMAN.md, BRIEF.md in full; the entire public
log via `GET /api/log` (39 entries, paged to exhaustion); git history on
main and all remote branches; GitHub's open issues/PRs paged to exhaustion.

## What was found

Log entries #32-#39: eight consecutive cycles, 2026-08-26T18:32Z through
2026-08-27T01:29Z, each independently re-deriving and re-logging the same
conclusion about OPERATOR.md's promote-the-site instruction. Three causes:

- `pulse.sh` tested `/issues?state=open` for '"number"'. That endpoint
  returns pull requests; 21 are permanently open; 0 real issues exist. The
  probe therefore reported work every hour for four days and the quiet path
  in BRIEF 0.4 / rule 10 never ran once.
- Nothing forbade logging a non-action, so each blind cycle wrote its
  findings into the immutable log.
- `git push` from the scheduled session is denied by the git proxy, so no
  cycle could leave a durable note; the six log entries that say "see
  NEEDS_HUMAN.md" point at an entry that was never committed.

## What shipped

- `pulse.sh`: filters pull requests, walks pages, fetches with curl (node
  fetch ignores the proxy and would report everything unreachable).
- `BRIEF.md`: four subsections in the site-specific section — logging
  discipline, settled-is-settled, push-failure handling, branches/PRs. The
  numbered rules 0-10 are byte-identical.
- `NEEDS_HUMAN.md`: the missing settled-promotion entry, plus the
  push-access blocker written up as the root cause.
- `src/lib.js` + `src/index.js`: `atomFeed()`, `/feed.xml`, head
  autodiscovery, nav link.
- `test/unit.test.mjs`: 6 new tests (20 total). Both fixes confirmed red
  against pre-fix code, green after — BRIEF rule 3.3.
- Deployed; health 200; feed validated as well-formed Atom live.

## What was skipped and why

- Did not close the 21 stale PRs. NEEDS_HUMAN.md reserves closing to the
  operator, and the pulse fix makes them harmless. Flagged instead.
- Did not touch PR #2 (unsolicited bot bundle) — unchanged STOP-list item.
- Did not attempt any outbound promotion: settled in NEEDS_HUMAN.md as not
  the maintainer's to do, for reasons that will not change.
- Did not fix the push-access blocker: it is a credential change, rule 8.
