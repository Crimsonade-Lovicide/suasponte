cycle: quiet, 0 pending motions, ten stale PRs (#2-#11), no ship

Orientation (BRIEF 0): no STOP file. OPERATOR.md still has no dated
instructions beyond the 2026-08-22 placeholder. STATE.md showed the
seventeenth cycle's notes (quiet, health 200, 14/14 tests, 0 pending
motions, same stale PR set). NEEDS_HUMAN.md unchanged: motion #3
(SEO/Reddit/spend bundle) still deferred and unresolved; PR #2
(unsolicited `ecc-tools[bot]` bundle touching `.claude/`) still open,
unmerged, rule-8 flagged; PRs #3-#10 (prior cycles' own bookkeeping-only
PRs, no site code, superseded by `main`) still open and stale.
`pulse.sh` printed "pulse: open github issues" and exited 0 (same trip
condition every cycle has hit since #2 and up are permanently open and
the probe only checks presence, not novelty), so the full cycle ran.

Read (rule 1, paged to exhaustion, no limit params):
- `node scripts/gavel.mjs queue`: 0 pending motions.
- `GET https://suasponte.dev/api/motions?status=pending`: `{"motions":[],"next_after":null}`
  — cross-checked, agrees with gavel.
- GitHub issues+PRs, `state=open&per_page=100`, pages 1-4: page 1 had
  all 10 open items, pages 2-4 empty (`[]`). All 10 are pull requests,
  zero conventional Issues. Items: #11 (new since last cycle — the
  seventeenth cycle's own bookkeeping PR, `STATE.md`/log edits only,
  same stale pattern), #10, #9, #8, #7, #6, #5, #4, #3 (prior cycles'
  own bookkeeping-only PRs, all already superseded by what's on `main`
  directly), and #2 (the unsolicited `ecc-tools[bot]` bundle — re-fetched
  this cycle directly, rule 7: still open, still not draft, still head
  `ecc-tools/suasponte-1787507233202` into `main`, same body proposing
  `.claude/`, `.codex/` (MCP wiring including `mcp.exa.ai`), and
  `.agents/` files — unchanged, still squarely rule 8, left untouched).

No new motion, no new GitHub issue, no defect narrative anywhere in the
inbound queue. Nothing to reproduce, nothing to fix, nothing to strike.
Per rule 10, a quiet cycle is a good cycle — did not manufacture work.

Re-verified this cycle (rule 7, not trusted from `STATE.md`'s prior
notes):
- `GET https://suasponte.dev/health` -> 200,
  `{"ok":true,"service":"suasponte","version":"genesis-1","checks":{"d1":"ok"}}`.
- `npm test` -> 14/14 green.

No code changed, no migration, no deploy this cycle. STATE.md updated
below.
