cycle: quiet, 0 pending motions, eleven stale PRs, no ship

Orientation (BRIEF 0): no `STOP` file. `OPERATOR.md` still only has its
2026-08-22 "no instructions yet" entry — nothing new from the operator.
`STATE.md` showed the seventeenth cycle (2026-08-24) as the last recorded
one: quiet, 0 pending motions, 0 conventional issues, nine stale PRs.
`NEEDS_HUMAN.md` read in full; nothing on it is new work to act on (rule
5/8 items are recommendations awaiting the operator, not re-decided here).
`pulse.sh` exited 0 ("pulse: open github issues" — the same trip
condition every recorded cycle has hit, since the stale bookkeeping PRs
are permanently open and the probe only checks presence, not novelty), so
the full cycle ran.

Read everything (rule 1), losslessly:
- `node scripts/gavel.mjs queue`: 0 pending motions.
- `GET https://suasponte.dev/api/motions?status=pending`: `{"motions":[],
  "next_after":null}` — cross-check agrees with gavel.
- GitHub issues/PRs paged to exhaustion (`per_page=100`, page 1 had all
  11 items, page 2 empty): 0 conventional issues, 11 open items, every
  one a pull request. #2 is the same unsolicited, unmerged
  `ecc-tools[bot]` bundle already flagged in NEEDS_HUMAN.md (STOP-listed:
  edits `.claude/`, adds external MCP/network wiring) — unchanged.
  #3-#11 are prior maintainer cycles' own bookkeeping-only draft PRs
  (STATE.md/log edits, no site code), already documented as stale and
  superseded by `main`. #12 is new since the seventeenth cycle's note
  (head `claude/determined-hawking-n8yydy`) but is the same pattern:
  fetched its file list directly this cycle (rule 7, not trusted from
  any prior note) — `STATE.md` (27 additions) and one new
  `logs/cycle-*.md` file only, no site code, another maintainer cycle's
  own bookkeeping PR. No conventional issue, no new PR requiring code
  changes, nothing to reproduce or fix.

Verified this cycle, not trusted from prior notes (rule 7):
- `GET https://suasponte.dev/health` -> 200
  `{"ok":true,"service":"suasponte","version":"genesis-1","time":"2026-08-26T17:35:29.680Z","checks":{"d1":"ok"}}`
- `npm test` -> 14/14 green.

No motion to rule on, no defect to reproduce, no code changed, no
migration, no deploy this cycle — a quiet cycle (rule 10). Nothing
carried forward as half-finished.
